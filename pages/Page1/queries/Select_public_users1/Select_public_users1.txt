SELECT
  *
FROM
  public.users
WHERE
  "gender" ilike '%{{Grades.searchText}}%'
ORDER BY
  "{{Grades.sortOrder.column || 'id'}}" {{Grades.sortOrder.order !== "desc" ? "" : "DESC"}}
LIMIT
  {{Grades.pageSize}}
OFFSET
  {{Grades.pageOffset}}